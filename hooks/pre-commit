#!/usr/bin/env python3
"""Require encrypted secret files in the Git index."""

import subprocess
import sys


AGE_HEADER = b"-----BEGIN AGE ENCRYPTED FILE-----"


def git(*args):
    return subprocess.check_output(["git", *args], stderr=subprocess.PIPE)


def is_host_secret(path):
    parts = path.split("/")
    return (
        len(parts) >= 5
        and parts[0] == "hosts"
        and parts[2] == "ansible"
        and parts[3] == "secrets"
    )


def is_workload_file(path, filename):
    parts = path.split("/")
    return (
        len(parts) == 5
        and parts[0] == "hosts"
        and parts[2] == "workloads"
        and parts[4] == filename
    )


def is_workload_secret(path):
    parts = path.split("/")
    return (
        len(parts) >= 6
        and parts[0] == "hosts"
        and parts[2] == "workloads"
        and parts[4] == "secrets"
    )


def main():
    paths = git("ls-files", "--cached", "-z")
    rejected_plaintext = []
    rejected_age = []

    for raw_path in paths.split(b"\0"):
        if not raw_path:
            continue
        path = raw_path.decode("utf-8", errors="surrogateescape")
        if (
            (is_host_secret(path) and not path.endswith((".age", ".example")))
            or is_workload_file(path, ".env")
            or (is_workload_secret(path) and not path.endswith((".age", ".example")))
        ):
            rejected_plaintext.append(path)
        elif (
            (is_host_secret(path) and path.endswith(".age"))
            or is_workload_file(path, ".env.age")
            or (is_workload_secret(path) and path.endswith(".age"))
        ):
            if not git("show", f":{path}").startswith(AGE_HEADER):
                rejected_age.append(path)

    if rejected_plaintext:
        print("Commit blocked: staged plaintext secret files are not allowed:", file=sys.stderr)
        for path in rejected_plaintext:
            print(f"  {path!r}", file=sys.stderr)

    if rejected_age:
        print("Commit blocked: staged secret ciphertext must be ASCII-armored age files:", file=sys.stderr)
        for path in rejected_age:
            print(f"  {path!r}", file=sys.stderr)

    if rejected_plaintext or rejected_age:
        print(
            "Run make encrypt-secrets to refresh .age ciphertext with age --armor, "
            "and keep plaintext secret files out of Git.",
            file=sys.stderr,
        )
        return 1
    return 0


if __name__ == "__main__":
    try:
        sys.exit(main())
    except subprocess.CalledProcessError:
        print("Commit blocked: unable to inspect staged secrets.", file=sys.stderr)
        sys.exit(1)
